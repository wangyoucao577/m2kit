name: Build 

on: 
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - '**'
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

env:
  CONAN_PRINT_RUN_COMMANDS: 1

jobs:
  build:
    strategy:
      matrix:
        build_params: 
          - ""  # default gcc
          # - "-s compiler=clang -s compiler.version=12"
          - "-s arch=armv7 -s os=Android -s os.api_level=16 -s compiler=clang -s compiler.version=9 -s compiler.libcxx=c++_static"
          - "-s arch=armv8 -s os=Android -s os.api_level=21 -s compiler=clang -s compiler.version=9 -s compiler.libcxx=c++_static"
          - "-s arch=x86_64 -s os=Android -s os.api_level=21 -s compiler=clang -s compiler.version=9 -s compiler.libcxx=c++_static"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: 'true'

      # setup build tools: ninja, conan
      - name: Setup build requirements
        run: sudo apt-get install ninja-build yasm
      - name: Install Conan
        id: conan
        uses: turtlebrowser/get-conan@main
      - name: Check tools
        run: conan --version && ninja --version && echo $PATH

      # build phase
      - name: Build
        run: | 
          python build.py ${{ matrix.build_params }}

      # view built conan packages
      - name: View built packages
        run : |
          conan search "*"

  build-on-mac:
    strategy:
      matrix:
        build_params: 
          - ""  # host macos
          - "-s arch=armv7 -s os=iOS -s os.version=9.0"
          - "-s arch=armv8 -s os=iOS -s os.version=9.0"
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: 'true'

      # setup build tools: ninja, conan
      - name: Setup build requirements
        run: brew install ninja yasm
      - name: Install Conan
        id: conan
        uses: turtlebrowser/get-conan@main
      - name: Check tools
        run: conan --version && ninja --version && echo $PATH

      # build phase
      - name: Build
        run: | 
          python build.py ${{ matrix.build_params }}

      # view built conan packages
      - name: View built packages
        run : |
          conan search "*"